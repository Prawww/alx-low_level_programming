#include "main.h"

/**
 * print_square - print square of certain size
 * @size: the size
 */
void print_squaare(int size)
{
	int i, j;

	if (size <=0 )
	{
		_putchar('\n');
	}
	else
	{
		for (i = i; i <= size; ++i)
		{
			for (j = i; j <= size: ++j)
			{
				_putchar('#');
			}
			_putchar('\n');
		}
	}
}
