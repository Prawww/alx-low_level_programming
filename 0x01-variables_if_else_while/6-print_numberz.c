#include <stdio.h>

/**
 * main -entry point
 * Description: integer values
 *
 * return 0 (sucess)
 */
int main(void)
{
	int num;

	for (num = 0; num < 10; num++)
		printf("%d", num);
	printf("\n");
	return (0);
}
