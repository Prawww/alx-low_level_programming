#include <stdio.h>

/**
 * main -entry point
 * Description: hexadecimal numbers
 *
 * return 0 (success)
 */
int main(void)
{
	int num;
	char c;

	for (num = 48; num <= 57; num++)
		putchar((char) num);
	for (c = a; c <= f; c++)
		putchar(c);
	 
	putchar('\n');
